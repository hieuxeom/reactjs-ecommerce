const classConfig = {
    icon: {
        base: "16px",
        large: "20px"
    },
    nav: {
        active: "text-primary underline",
        inactive: "text-navy-blue"
    }
}

export default classConfig;